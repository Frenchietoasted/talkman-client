{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@clerk/express": "catalog:",
    "@hono/trpc-server": "^0.4.2",
    "@talkman/api": "workspace:*",
    "@talkman/db": "workspace:*",
    "@talkman/env": "workspace:*",
    "@trpc/server": "catalog:",
    "cors": "^2.8.6",
    "dotenv": "catalog:",
    "express": "^5.2.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@talkman/config": "workspace:*",
    "@types/bun": "catalog:",
    "@types/cors": "^2.8.19",
    "@types/express": "catalog:",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  },
  "trustedDependencies": [
    "supabase"
  ]
}
